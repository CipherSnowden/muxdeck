name: muxdeck_protocol
description: >-
  MuxDeck wire protocol types, shared by the mobile deck and the desktop control
  panel. An implementation of docs/PROTOCOL.md, which is the source of truth.
version: 0.1.0
repository: https://github.com/CipherSnowden/muxdeck
publish_to: none

environment:
  sdk: ^3.12.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
