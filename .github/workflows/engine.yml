name: engine

on:
  push:
    branches: [main]
    paths:
      - 'engine/**'
      - 'protocol/fixtures/**'
      - '.github/workflows/engine.yml'
  pull_request:
    paths:
      - 'engine/**'
      - 'protocol/fixtures/**'
      - '.github/workflows/engine.yml'

permissions:
  contents: read

concurrency:
  group: engine-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: engine

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      # No toolchain action: engine/rust-toolchain.toml pins the version and the
      # rustfmt/clippy components, and rustup honours it automatically. Installing a
      # toolchain here as well would just mask a drift between CI and the dev machine.
      - name: Show toolchain
        run: rustup show

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: engine

      - name: Format
        run: cargo fmt --all --check

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Test
        run: cargo test --workspace

      # The uinput tests are #[ignore]d because they need write access to /dev/uinput, which no
      # ordinary machine grants. A Linux runner can, and this is the only automated proof that
      # the Linux backend emits the right key codes in the right order rather than merely
      # compiling.
      #
      # Run as root rather than chmodding /dev/uinput: the read-back test also has to open the
      # /dev/input/event* node the kernel creates, and that one is root:input 0640 and does not
      # exist yet to be chmodded. `usermod -aG input` would need a fresh login to take effect.
      # A separate target directory keeps root-owned artefacts out of the cached one.
      - name: Test uinput injection
        if: matrix.os == 'ubuntu-latest'
        env:
          CARGO_TARGET_DIR: /tmp/uinput-target
        run: |
          sudo modprobe uinput
          sudo -E env "PATH=$PATH" "HOME=$HOME" cargo test -p muxdeck-input -- --ignored --test-threads=1
